- question_name = custom_field_value.question.name(I18n.locale)
%b= "#{question_name}:"
- text_with_url_links do
  = custom_field_value.text_value
- val = custom_field_value.text_value
- if val.include?("https")
  %a{href: val}
    = val
- else
  = val
